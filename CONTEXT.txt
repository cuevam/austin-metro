PROJECT: Austin Metro Transit Data Warehouse
Portfolio project — Senior Data Engineering
============================================================

GOAL:
Build a real-time transit telemetry pipeline for Capital Metro (Austin, TX) buses,
from raw API ingestion through to a custom Next.js + deck.gl visualization layer.

------------------------------------------------------------
STACK
------------------------------------------------------------
Warehouse:      ClickHouse (columnar DB, Docker on WSL2)
Orchestrator:   Dagster (asset-based pipeline, schedule every minute)
Visualization:  Next.js 14 app (replacing Grafana) — transit-map/
Language:       Python 3 (venv on WSL2), TypeScript (Next.js)

Data Source:    Capital Metro GTFS-Realtime feed (vehicle positions)
                Texas Open Data Portal — JSON API, updates every ~15 seconds
                URL: https://data.texas.gov/download/cuc7-ywmd/text%2Fplain

Static GTFS:    CapMetro static schedule ZIP
                URL: https://data.texas.gov/download/r4v4-vz24/application%2Fzip

------------------------------------------------------------
PORTS
------------------------------------------------------------
Dagster UI:     http://localhost:3000
Transit Map:    http://localhost:3001   (Next.js dev server)
Grafana:        http://localhost:3002   (kept but replaced by transit-map)
ClickHouse:     http://localhost:8123   (HTTP), 9000 (native)

------------------------------------------------------------
PIPELINE ARCHITECTURE (Medallion / Lakehouse Pattern)
------------------------------------------------------------

BRONZE LAYER — bronze_transit_snapshot (Dagster asset)
  - Hits the CapMetro GTFS-RT API
  - Dumps raw JSON blob as-is into ClickHouse table: bronze_raw_json
  - Runs every minute via Dagster schedule (currently stopped, manual trigger)
  - Table engine: MergeTree, ordered by ingested_at (append-only landing zone)

SILVER LAYER — silver_telemetry (Dagster asset, depends on bronze)
  - Uses ClickHouse JSONExtract* functions to shred raw JSON into clean typed rows
  - Watermark pattern: only processes bronze rows newer than max(source_ingested_at)
  - Fields: vehicle_id, vehicle_label, trip_id, route_id, direction_id, start_time,
            start_date, schedule_relationship, latitude, longitude, bearing,
            speed_mps, speed_kph, current_stop_sequence, current_status,
            stop_id, vehicle_timestamp, source_ingested_at, processed_at
  - Table engine: ReplacingMergeTree, ordered by (vehicle_id, vehicle_timestamp)
  - Deduplicates repeated pings for the same vehicle

GOLD LAYER — gold_active_fleet (ClickHouse View)
  - Joins silver_telemetry with dimension tables
  - Uses argMax() to get the latest ping per vehicle within the last 15 minutes
  - Enriches with: route_short_name, route_long_name, route_color, trip_headsign
  - Source tables: silver_telemetry, dim_routes, dim_trips
  - NOTE: Column t.route_id must be referenced with backtick escaping in queries
    because the view exposes it as "t.route_id" (CTE alias leak in ClickHouse)

DIMENSIONS — static_gtfs_dimensions (separate Dagster job, manual trigger)
  - Downloads CapMetro static GTFS ZIP
  - Parses CSV files inside ZIP: routes.txt, stops.txt, trips.txt, shapes.txt
  - Loads into ClickHouse: dim_routes, dim_stops, dim_trips, dim_shapes
  - Full truncate + reload on each run (static data, infrequent changes)

------------------------------------------------------------
CLICKHOUSE TABLES
------------------------------------------------------------
bronze_raw_json     raw_payload String, ingested_at DateTime
silver_telemetry    typed telemetry columns (see above)
dim_routes          route_id, route_short_name, route_long_name, route_color
dim_stops           stop_id, stop_name, stop_lat, stop_lon
dim_trips           trip_id, route_id, direction_id, trip_headsign
dim_shapes          shape_id, shape_pt_lat, shape_pt_lon, shape_pt_sequence
gold_active_fleet   VIEW — latest ping per vehicle enriched with route/trip info

------------------------------------------------------------
DAGSTER DEFINITIONS (definitions.py)
------------------------------------------------------------
Assets:     bronze_transit_snapshot, silver_telemetry, static_gtfs_dimensions
Jobs:       transit_sync_job (bronze + silver), dimension_sync_job (dimensions)
Schedules:  transit_schedule — cron "* * * * *", default status STOPPED

------------------------------------------------------------
DOCKER SERVICES (docker-compose.yml)
------------------------------------------------------------
clickhouse:   ports 8123 (HTTP), 9000 (native) | data persisted in ./ch_data
grafana:      port 3002 | ClickHouse plugin installed | data in ./grafana_data

------------------------------------------------------------
TRANSIT MAP — Next.js App (transit-map/)
------------------------------------------------------------
Framework:    Next.js 14, App Router, TypeScript
Dev port:     3001 (changed from default 3000 to avoid Dagster conflict)
Planned viz:  deck.gl animated bus map

API Routes:
  GET /api/fleet
    - Queries gold_active_fleet directly via ClickHouse HTTP API (no SDK)
    - Uses fetch() with X-ClickHouse-User / X-ClickHouse-Key headers
    - Returns FleetVehicle[] as JSON (395 buses when pipeline is live)
    - Credentials loaded from transit-map/.env.local (not committed)

FleetVehicle shape:
  vehicle_id, latitude, longitude, bearing, speed_kph, current_status,
  last_seen_at, route_id, route_short_name, route_long_name, route_color,
  trip_headsign

.env.local (transit-map/):
  CLICKHOUSE_URL=http://localhost:8123
  CLICKHOUSE_USER=default
  CLICKHOUSE_PASSWORD=vanguard_pass

------------------------------------------------------------
CURRENT STATUS
------------------------------------------------------------
[X] Infrastructure:       Docker/WSL2 permissions stable
[X] Bronze Layer:         Raw JSON snapshots landing via Dagster asset
[X] Silver Layer:         JSON shredding + watermark incremental processing working
[X] Dimensions:           GTFS static tables loading successfully
[X] Gold View:            gold_active_fleet returning 395 live buses
[X] Next.js scaffold:     transit-map/ app created, port set to 3001
[X] /api/fleet route:     Live ClickHouse query working, returns real fleet data
[ ] deck.gl map:          Not yet built — Phase 2 next
[ ] Dagster Schedule:     Defined but not activated (running manually)

------------------------------------------------------------
CODE HISTORY / SESSION LOG
------------------------------------------------------------

[Session 1 — Repo Setup]
  - Initialized git repo, created .gitignore (excluded .env, venv, grafana_data,
    ch_data, __pycache__, .tmp_dagster_home*)
  - Created GitHub repo: https://github.com/cuevam/austin-metro
  - Initial commit: pipeline code, SQL schema, Dagster definitions, docker-compose
  - Added CONTEXT.txt to document project state

[Session 2 — transit-map Phase 1: Scaffold]
  - Created branch: feat/transit-map (master stays as stable pipeline baseline)
  - Scaffolded Next.js 14 app in transit-map/ (app router, TypeScript, no Tailwind)
  - Created transit-map/app/api/fleet/route.ts with hardcoded FleetVehicle[] stub
  - FleetVehicle interface designed to match gold_active_fleet schema exactly
  - Changed dev port from 3000 → 3001 in package.json to avoid Dagster conflict

[Session 2 — transit-map Phase 2: Live Data]
  - Created transit-map/.env.local with ClickHouse credentials
  - Rewrote /api/fleet route to query ClickHouse HTTP API directly via fetch()
  - Debugged: gold_active_fleet exposes route_id as "t.route_id" (CTE alias leak)
    Fix: reference column as \`t.route_id\` with escaped backticks in SQL string
  - Confirmed: /api/fleet returns 395 live buses from gold_active_fleet
  - Committed and pushed to feat/transit-map

------------------------------------------------------------
NEXT STEPS
------------------------------------------------------------
1. Phase 3: Build deck.gl map in transit-map/ rendering buses as animated icons
2. Activate Dagster schedule for continuous ingestion
3. Consider adding route path overlays using dim_shapes

------------------------------------------------------------
REPO: https://github.com/cuevam/austin-metro
BRANCH: feat/transit-map (active)
MASTER: stable pipeline baseline — do not break
============================================================
